{
  "name": "kancolle-perf-testing",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "devDependencies": {
    "@playwright/test": "^1.29.1",
    "artillery": "^2.0.0-27",
    "artillery-engine-playwright": "^0.2.1"
  },
  "scripts": {
    "test:lt": "mkdir -p ./test-results && yarn artillery run --output ./test-results/test-run-report.json",
    "report:lt": "yarn artillery report --output ./test-results/test-run-report.html"
  }
}
